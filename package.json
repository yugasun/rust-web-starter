{
  "name": "rust-web-starter",
  "version": "0.1.0",
  "description": "Rust Web Application Starter with tailwindcss",
  "scripts": {
    "setup": "concurrently \"npm run setup:npm\" \"npm run setup:yew\"",
    "setup:npm": "rustup target add wasm32-unknown-unknown && cargo install --locked trunk && npm install",
    "setup:yew": "rustup target add wasm32-unknown-unknown && cargo install --locked trunk && npm install",
    "dev": "concurrently \"npm run dev:css\" \"npm run dev:serve\"",
    "dev:css": "tailwindcss -c tailwind.config.js -o tailwind.css --watch",
    "dev:serve": "trunk serve",
    "prod": "npm run prod:css && trunk build --release",
    "prod:css": "tailwindcss -c tailwind.config.js -o tailwind.css --minify"
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "daisyui": "^4.12.10",
    "tailwindcss": "^3.4.10"
  }
}